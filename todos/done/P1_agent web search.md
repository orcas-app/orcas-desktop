# Enable web search

- Add an optional configuration to Agents to enable web search. The user should be able to choose whether a given agent has the ability to search the web.
- Add support for displaying `<search_web>` and `<query>` blocks in message responses